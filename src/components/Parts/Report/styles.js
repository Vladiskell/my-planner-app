import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles((theme) => ({
    report: {
        height: '100%',
        maxHeight: '100%',
        overflowY: 'auto',
    }
}))

