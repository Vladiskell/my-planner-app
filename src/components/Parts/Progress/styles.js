import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles((theme) => ({
    progress: {
        display: 'flex',
        alignItems: 'center',
        height: '100%',
        padding: '0 16px',
    },
}))
