import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
    description: {
        height: '100%',
    },
    descriptionBody: {
        padding: '16px 24px',
    },
    iconButton: {
        color: '#fff',
        marginLeft: 'auto',
        marginRight: -8,
    },
}));
